# shellcheck shell=bash
# .bash_profile — login shell entry point
# Source .bashrc so interactive settings apply in tmux (which starts login shells)
# shellcheck source=/dev/null
[ -f ~/.bashrc ] && . ~/.bashrc
